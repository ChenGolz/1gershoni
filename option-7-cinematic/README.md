# קולנוע זיכרון | סוניה ואמיר פלוט

This folder is standalone. Upload the entire folder contents to the chosen page/location.

Before publishing on the association site:
1. Replace the placeholder links in the header: `/` and `/memorials/`.
2. Replace the logo placeholder with the association logo if available.
3. Replace `og:image` with the full absolute URL after upload, if your CMS requires it.
4. Keep `assets/docs/` only if the PDF farewell links should remain available.
